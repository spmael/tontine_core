---
description: Plan small implementation tasks for a tontine-core capability.
---

Capability: ${input:capability:Capability file path}

Read the capability, requirements, BRD sections, and existing tasks. Propose small `TASK-<DOMAIN>-NNN` records with requirements, acceptance criteria, dates, tests, and exact `uv run` validation commands. Present the plan before changing work to `in_progress`.
