---
description: Create an architecture decision record for tontine-core.
---

Decision: ${input:decision:Short decision title}

Create the next `docs/architecture/adr/ADR-NNN-<slug>.md` from the ADR template. Record context, decision, alternatives, consequences, verification, dates, and affected `adr_refs`. Regenerate the reverse index with `uv run python .agents/skills/pdlc/scripts/adr_index.py`.
