---
description: Execute one tontine-core task with tests, checked acceptance criteria, and a completion record.
---

Task: ${input:task:Path to task file}

Follow the pdlc and python-standards skills. Set `in_progress` and `started`, implement test-first where practical, validate with `uv run`, check every satisfied criterion, create `docs/planning/completions/TASK-<ID>.md`, and mark `shipped` only with complete evidence. Do not commit automatically.
