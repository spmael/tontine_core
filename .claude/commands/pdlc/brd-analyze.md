---
description: Refresh the package requirements register from the BRD.
argument-hint:
---

Read `BRD_tontine_core_v1.md` in full and compare it with
`docs/planning/requirements.md`.

1. Preserve existing requirement IDs.
2. Add only requirements genuinely stated by the BRD.
3. Record functional and non-functional requirements with stable `FR-*` and
   `NFR-*` identifiers.
4. Check that every requirement maps to a capability or explicitly report it as
   uncovered.
5. Do not delete or renumber requirements silently.
6. Present additions, removals requiring confirmation, and traceability changes
   for review before editing downstream tasks.
